% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mg2_demo.R
\name{mg2_demo_dir}
\alias{mg2_demo_dir}
\title{Path to the MG2 demo data directory}
\usage{
mg2_demo_dir()
}
\value{
Character scalar — absolute path to \verb{inst/extdata/demo/} in the
installed package.
}
\description{
Returns the path to the bundled demo dataset directory inside the installed
package.  The directory contains three files that mirror a real MG2 data
folder and can be pointed at directly from the Shiny app's Setup tab or
used in standalone R scripts via \code{\link[=read_file]{read_file()}}.
}
\details{
\strong{Contents of the demo directory}\tabular{ll}{
   File \tab Description \cr
   \code{malaria_demo.rds} \tab 6-year Lao PDR malaria surveillance data (Aug 2021 – May 2026), 18 columns, ~867 k facility-month rows \cr
   \code{metadata_demo.rds} \tab DHIS2 metadata: org unit tree, geographic features (sf), validation rules, data element dictionary \cr
   \code{Formulas_demo.xlsx} \tab Formula file listing 67 malaria data elements across 18 categories \cr
}


\strong{Data source}

Data were downloaded from the public DHIS2 demonstration server at
\url{https://demos.dhis2.org/hmis_data} (Lao PDR national malaria programme
configuration).  This is demonstration data intended for training and
software testing; it is not the official data of the Lao Ministry of Health.

\strong{Columns in \code{malaria_demo.rds}}\tabular{lll}{
   Column \tab Type \tab Description \cr
   \code{orgUnit} \tab character \tab DHIS2 org unit UID \cr
   \code{orgUnitName} \tab character \tab Facility / district / province name \cr
   \code{level} \tab integer \tab 2 = province, 3 = district, 4 = facility \cr
   \code{Country}, \code{Province}, \code{District}, \code{Facility} \tab character \tab Geographic hierarchy labels \cr
   \code{effectiveLeaf} \tab logical \tab \code{TRUE} for the lowest level that reported data \cr
   \code{Month} \tab yearmonth \tab Reporting month (tsibble \code{yearmonth} class) \cr
   \code{data} \tab character \tab Data element name (human-readable, from formula) \cr
   \code{original} \tab numeric \tab Raw reported value from DHIS2 \cr
   \code{key_entry_error} \tab logical \tab Flagged as mobile-phone key-entry error \cr
   \code{over_max} \tab logical \tab Exceeds theoretical maximum (e.g. > 31/month) \cr
   \code{mad15} \tab logical \tab > 15 × median absolute deviation \cr
   \code{mad10} \tab logical \tab > 10 × MAD (after mad15 removed) \cr
   \code{mad5} \tab logical \tab > 5 × MAD (after mad10 removed) \cr
   \code{seasonal5} \tab logical \tab > 5 × seasonal decomposition residual / MAD \cr
   \code{seasonal3} \tab logical \tab > 3 × seasonal decomposition residual / MAD \cr
}
}
\examples{
demo <- mg2_demo_dir()
list.files(demo)

# Load the processed data
d <- read_file(file.path(demo, "malaria_demo.rds"))

# Load the metadata
meta <- readRDS(file.path(demo, "metadata_demo.rds"))
names(meta)

}
