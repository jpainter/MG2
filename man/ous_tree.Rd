% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dhis2_api.R
\name{ous_tree}
\alias{ous_tree}
\title{Build an Org Unit Hierarchy Table}
\usage{
ous_tree(ous, ouLevels, .verbose = FALSE)
}
\arguments{
\item{ous}{Data frame of org units as returned by the DHIS2
\code{organisationUnits} API endpoint. Must contain columns \code{id}, \code{name},
\code{parent.id}, \code{level}, and \code{leaf}.}

\item{ouLevels}{Data frame of org unit levels with columns \code{level} (integer)
and \code{levelName} (character).}

\item{.verbose}{Logical. Print progress messages (default: \code{FALSE}).}
}
\value{
A tibble with one row per org unit. Columns: \code{orgUnit} (id),
\code{orgUnitName}, one column per admin level named by \code{levelName},
\code{level}, and \code{leaf}.
}
\description{
Converts a flat org unit list (from the DHIS2 API) and a levels table into
a wide-format hierarchy table. Each row represents one org unit and columns
give the names of its ancestors at each administrative level. Uses
\code{data.tree} to traverse the parent-child network.
}
\examples{
\dontrun{
  tree <- ous_tree(ous = org_units_df, ouLevels = levels_df)
}
}
