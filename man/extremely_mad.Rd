% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{extremely_mad}
\alias{extremely_mad}
\title{Flag Values Exceeding a MAD-Based Threshold}
\usage{
extremely_mad(
  x,
  deviation = 15,
  smallThreshold = 50,
  key_entry_error = NA,
  over_max = NA,
  maximum_allowed = NA,
  logical = FALSE,
  .progress = FALSE,
  total = NA
)
}
\arguments{
\item{x}{Numeric vector.}

\item{deviation}{Numeric. Multiple of MAD used as threshold (default: \code{15}).}

\item{smallThreshold}{Numeric. Series with all values ≤ this are skipped
(default: \code{50}).}

\item{key_entry_error}{Logical vector. Pre-flagged key-entry errors (unused
internally; kept for API compatibility).}

\item{over_max}{Logical vector. Pre-flagged over-max values (unused
internally; kept for API compatibility).}

\item{maximum_allowed}{Numeric. Pre-flagged maximum cap (unused internally).}

\item{logical}{Logical. When \code{TRUE} return a logical flag vector; when
\code{FALSE} return values with non-outliers set to \code{NA} (default: \code{FALSE}).}

\item{.progress}{Logical. Update Shiny progress bar (default: \code{FALSE}).}

\item{total}{Integer. Total series count for progress increment calculation.}
}
\value{
Logical or numeric vector the same length as \code{x}.
}
\description{
For a numeric vector \code{x}, computes the median and median absolute deviation
(MAD), then flags values that deviate from the median by more than
\verb{deviation × MAD}. When MAD is near zero, falls back to a trimmed SD.
Returns \code{NA} for all values when the series is entirely below
\code{smallThreshold}.
}
