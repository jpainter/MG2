% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_functions.R
\name{apply_combine_cleaning}
\alias{apply_combine_cleaning}
\title{Apply outlier cleaning at a specified cumulative severity level}
\usage{
apply_combine_cleaning(d, cleaning_level = "seasonal3")
}
\arguments{
\item{d}{data.frame or data.table with \code{original}, optional \code{expected}, and
outlier flag columns.}

\item{cleaning_level}{One of \code{"none"}, \code{"mad15"}, \code{"mad10"}, \code{"seasonal5"},
\code{"seasonal3"}.}
}
\value{
Numeric vector of cleaned values, same length as \code{nrow(d)}.
}
\description{
Replaces values flagged as outliers  -  at or below the chosen severity  -  with
the modelled \code{expected} value (or \code{NA} when \code{expected} is unavailable).
}
\details{
Severity order (most -> least severe):
\code{key_entry_error} > \code{over_max} > \code{mad15} > \code{mad10} > \code{seasonal5} > \code{seasonal3}

"Use mad15" means: only replace values flagged by key_entry_error, over_max,
or mad15.  "Use seasonal3" replaces everything detected at any level.
}
