% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_functions.R
\name{chirps_download_tif}
\alias{chirps_download_tif}
\title{Download and cache a bbox-cropped CHIRPS monthly GeoTIFF}
\usage{
chirps_download_tif(year, month, bbox = NULL, cache_dir = tempdir())
}
\arguments{
\item{year}{Integer year (>= 1981).}

\item{month}{Integer month (1–12).}

\item{bbox}{Named numeric vector \code{c(xmin=, ymin=, xmax=, ymax=)} in
WGS84 degrees, or \code{NULL} to cache the full Africa TIF.}

\item{cache_dir}{Directory for caching downloaded files.}
}
\value{
Path to the cached \code{.tif} file (cropped when \code{bbox} is supplied).
}
\description{
The full continental/global TIF is downloaded to a \strong{temporary} file,
cropped immediately to \code{bbox} (plus a 0.2° buffer), and only the small
cropped version is written to \code{cache_dir}.  This mirrors the windowed-read
approach used by the original Python app (\code{rasterio.mask.mask(crop=True)})
while avoiding permanent storage of the large continental raster.
}
\details{
When \code{bbox} is \code{NULL} the full Africa TIF is cached (legacy/standalone use).

The function auto-selects the Africa-only dataset when the bbox falls within
Africa (~3-5 MB download) and the global dataset otherwise (~20 MB), so it
works for any country, including non-African DHIS2 instances (e.g. PDR Lao).
}
