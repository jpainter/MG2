% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden_functions.R
\name{burden_aci}
\alias{burden_aci}
\title{Burden Method E: Robust Malaria Incidence Estimator (champion facilities only)}
\usage{
burden_aci(
  data,
  confirmed_elements,
  attendance_elements,
  tested_elements,
  population_element = NULL,
  cat_map_attend = NULL,
  cat_map_tested = NULL,
  beta_young = 0.73,
  beta_old = 0.57,
  alpha = 0.48,
  gamma_young = 2,
  gamma_old = 1,
  lambdamin = 0.75,
  young_pattern = "<5|under.5|under 5|younger|<five",
  min_months = 10L,
  region_col,
  n_bootstrap = 1000L
)
}
\arguments{
\item{data}{data.table from \code{selectedData()} with a \code{Selected} column.}

\item{confirmed_elements}{character; \code{data} values for confirmed cases.}

\item{attendance_elements}{character; \code{data} values for total attendance.}

\item{tested_elements}{character; \code{data} values for patients tested.}

\item{population_element}{character or NULL; \code{data} value for population.}

\item{cat_map_attend}{named character; confirmed -> attendance category map.}

\item{cat_map_tested}{named character; confirmed -> tested category map.}

\item{beta_young}{numeric; beta for < 5 years (Thwing 2020 default 0.73).}

\item{beta_old}{numeric; beta for >= 5 years (Thwing 2020 default 0.57).}

\item{alpha}{numeric; TPR ratio untested/tested febrile (default 0.48).}

\item{gamma_young}{numeric; expected NMF episodes/person/year < 5 yrs (default 2.0).}

\item{gamma_old}{numeric; expected NMF episodes/person/year >= 5 yrs (default 1.0).}

\item{lambdamin}{numeric; minimum malaria-attributable fraction (default 0.75).}

\item{young_pattern}{regex matching "younger" category labels.}

\item{min_months}{integer; minimum reported months to qualify (default 10).}

\item{region_col}{character; region column name.}

\item{n_bootstrap}{integer; bootstrap draws.}
}
\value{
list with \verb{$subnational} and \verb{$national} data.tables.  When
population data are available each row also has \code{incidence_per_1k},
\code{incidence_lower}, \code{incidence_upper}.
}
\description{
Implements the full 5-equation Robust Estimator of Thwing, Plucinski,
Painter et al. (2020, AJTMH 102:811-820) to correct confirmed case counts
for incomplete testing coverage and, when population data are provided,
to estimate community-level incidence per 1,000 population.
}
\details{
Only champion facilities that consistently report all three required
elements (confirmed cases, total attendance, patients tested) are included.
Non-champion facilities are excluded  -  they do not influence the estimate.
Elements are summed across qualifying champions within each region before
the correction is applied.

Uncertainty is propagated by drawing beta, alpha, and lambdamin simultaneously from
uniform distributions spanning the published ranges from Thwing et al. 2020.
}
