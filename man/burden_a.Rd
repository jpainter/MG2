% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden_functions.R
\name{burden_a}
\alias{burden_a}
\title{Burden Method A: Champion Multiple}
\usage{
burden_a(
  data,
  target_elements,
  region_col,
  period_start = NULL,
  period_end = NULL,
  neighbor_list = NULL,
  n_bootstrap = 1000L
)
}
\arguments{
\item{data}{data.table from \code{selectedData()} (columns: orgUnit, Month,
data, value, Selected, and a region column).}

\item{target_elements}{character; which values of the \code{data} column to use.}

\item{region_col}{character; name of the region column (typically
\code{levelNames[2]}).}

\item{period_start}{yearmonth; start of the estimate window (NULL = all data).}

\item{period_end}{yearmonth; end of the estimate window (NULL = all data).}

\item{neighbor_list}{named list; maps each region to a character vector of
neighbouring region names used as a fallback when a region has no
champion facilities.}

\item{n_bootstrap}{integer; bootstrap draws (default 1000).}
}
\value{
list with \verb{$subnational} and \verb{$national} data.tables.
}
\description{
For each region and category, sample from the champion annual-total
distribution to predict non-champion facility totals.
}
