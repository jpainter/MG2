% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_functions.R
\name{chirps_load_rainfall}
\alias{chirps_load_rainfall}
\title{Load and combine all available flat rainfall RDS files from the data directory}
\usage{
chirps_load_rainfall(data_dir)
}
\arguments{
\item{data_dir}{The MG2 data directory.}
}
\value{
A data frame with columns \code{id}, \code{name}, \code{parentName}, \code{year},
\code{month}, \code{mean_rain}, or \code{NULL} if no files are found.
}
\description{
Scans \code{data_dir} for files matching \code{rainfall_*.rds}, reads each, and
row-binds them into a single data frame.  Files from different years and
admin levels are combined; duplicates (same \code{id} + \code{year} + \code{month}) are
de-duplicated by keeping the first occurrence (lowest level number, which
is typically finer-grained).
}
