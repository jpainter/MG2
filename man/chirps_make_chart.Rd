% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_functions.R
\name{chirps_make_chart}
\alias{chirps_make_chart}
\title{Build a ggplot2 bar chart of monthly CHIRPS rainfall}
\usage{
chirps_make_chart(
  results_list,
  year,
  title_prefix = "",
  group_by_parent = FALSE
)
}
\arguments{
\item{results_list}{Named list from \code{chirps_process_months()}.}

\item{year}{Integer year shown in the title.}

\item{title_prefix}{Character prefix for the chart title (e.g. country name
and admin level).}

\item{group_by_parent}{Logical. When \code{TRUE} and \code{parentName} is available,
colour and dodge bars by parent org unit. Silently ignored if \code{parentName}
is absent or has > 8 unique values.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a bar chart with mean rainfall per month (averaged across all
polygons in \code{results_list}). Error bars show ±1 SD across polygons.
When \code{group_by_parent} is \code{TRUE} and a \code{parentName} column is present with
≤ 8 unique values, bars are coloured and dodged by parent unit — useful for
showing regional rainfall variation within a country.
}
