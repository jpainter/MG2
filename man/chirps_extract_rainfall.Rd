% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/climate_functions.R
\name{chirps_extract_rainfall}
\alias{chirps_extract_rainfall}
\title{Extract mean CHIRPS rainfall for each polygon in an sf object}
\usage{
chirps_extract_rainfall(sf_obj, tif_path)
}
\arguments{
\item{sf_obj}{An \code{sf} polygon object.}

\item{tif_path}{Path to a CHIRPS monthly GeoTIFF (may already be cropped).}
}
\value{
\code{sf_obj} with added columns \code{mean_rain} (mm) and \code{valid_pixels}.
}
\description{
Mirrors Python's \verb{rasterio.mask.mask(src, [geom], crop=True)}: the raster
is first cropped to the sf bounding box so that only the relevant portion
is held in memory, then per-polygon zonal statistics are computed.
}
