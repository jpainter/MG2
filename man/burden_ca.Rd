% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden_functions.R
\name{burden_ca}
\alias{burden_ca}
\title{Burden Method CA: Cascade (C2 -> C1 -> A)}
\usage{
burden_ca(
  data,
  target_elements,
  region_col,
  period_start = NULL,
  period_end = NULL,
  min_months = 12L,
  n_bootstrap = 1000L
)
}
\arguments{
\item{data}{data.table from \code{selectedData()}  -  the \strong{full available history}.}

\item{target_elements}{character; target \code{data} values.}

\item{region_col}{character; region column name.}

\item{period_start, period_end}{yearmonth; estimate period window (NULL = all data).}

\item{min_months}{integer; minimum months of history for ARIMA (default 12).}

\item{n_bootstrap}{integer; simulation draws.}
}
\value{
list with \verb{$subnational}, \verb{$national}, and \verb{$not_modeled} (facilities
that used the A fallback).
}
\description{
Fits \code{auto.arima} on each facility's full monthly series (same as C2),
falls back to a linear model (same as C1), and when a facility cannot be
modeled by either, draws from the regional champion distribution (Method A)
instead of crediting actuals-only.  This makes CA a strictly higher or equal
estimate than C2 for facilities with insufficient history.
}
