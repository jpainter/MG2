% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{truncate_df_chars}
\alias{truncate_df_chars}
\title{Truncate Long Strings in a Data Frame}
\usage{
truncate_df_chars(df, limit = 500L, suffix = " …[TRUNC]")
}
\arguments{
\item{df}{A data frame or data.table.}

\item{limit}{Integer. Maximum number of characters to keep (default: 500).}

\item{suffix}{Character. Text appended to truncated values
(default: \code{" …[TRUNC]"}). Set to \code{NULL} or \code{""} to omit.}
}
\value{
The input \code{df} with long strings truncated. Returns invisibly.
}
\description{
Shortens character values that exceed \code{limit} characters. Truncated values
get a \code{suffix} appended to signal that the string was cut. Useful before
writing to Excel, which has a 32,767 character cell limit.
}
\examples{
df <- data.frame(x = paste(rep("word", 200), collapse = " "))
result <- truncate_df_chars(df, limit = 50)
}
