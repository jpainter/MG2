% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{fetch_validation_rules}
\alias{fetch_validation_rules}
\title{Fetch All Validation Rules from DHIS2}
\usage{
fetch_validation_rules(baseurl, username, password, id_names = NULL)
}
\arguments{
\item{baseurl}{Character. Base URL of the DHIS2 instance (with trailing \code{/}).}

\item{username}{Character. DHIS2 username.}

\item{password}{Character. DHIS2 password.}

\item{id_names}{A data frame with columns \code{id} and \code{name} used to translate
UID references in expressions. Typically a combined table of data elements,
indicators, and category option combos. Pass \code{NULL} to skip translation.}
}
\value{
A tibble with one row per validation rule and columns:
\code{id}, \code{name}, \code{description}, \code{importance}, \code{operator}, \code{ruleType},
\code{periodType}, \code{leftSide_description}, \code{leftSide_expression} (translated),
\code{rightSide_description}, \code{rightSide_expression} (translated),
\code{leftSide_expression_raw}, \code{rightSide_expression_raw} (original UIDs,
used internally for element-to-rule matching).
}
\description{
Downloads every validation rule from a DHIS2 instance, handling paged
responses automatically, and translates UID references in the left- and
right-side expressions to human-readable names.
}
