% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{seasonal_outliers}
\alias{seasonal_outliers}
\title{Flag Seasonal Outliers Across All Series in a tsibble}
\usage{
seasonal_outliers(
  d,
  .total = NULL,
  .threshold = 50,
  mad = "mad10",
  tests = c("seasonal5", "seasonal3"),
  progress = FALSE
)
}
\arguments{
\item{d}{A tsibble (output of \code{\link[=mad_outliers]{mad_outliers()}}).}

\item{.total}{Integer. Number of series (used for progress). If \code{NULL},
computed from \code{tsibble::n_keys(d)}.}

\item{.threshold}{Numeric. All-small threshold (default: \code{50}).}

\item{mad}{Character. Name of MAD flag column to exclude before seasonal
detection (default: \code{"mad10"}).}

\item{tests}{Character vector. Which seasonal tests to run; subset of
\code{c("seasonal5", "seasonal3")} (default: both).}

\item{progress}{Logical. Use \code{progressr} progress bar (default: \code{FALSE}).}
}
\value{
The input tsibble with \code{not_mad}, \code{seasonal5}, and \code{seasonal3}
columns added.
}
\description{
Applies \code{\link[=unseasonal]{unseasonal()}} to every series, flagging values that deviate from a
seasonal forecast by more than 5× MAD (\code{seasonal5}) and 3× MAD
(\code{seasonal3}).
}
