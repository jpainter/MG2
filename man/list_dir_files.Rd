% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{list_dir_files}
\alias{list_dir_files}
\title{List Files in a Directory Matching a Pattern}
\usage{
list_dir_files(search = "All", type = "xlsx", other = "", dir = NULL)
}
\arguments{
\item{search}{Character. Case-insensitive substring the filename must
contain (default: \code{"All"} matches everything).}

\item{type}{Character. Regex matched against the file extension, anchored
at end of string (default: \code{"xlsx"}). Use \code{"xlsx|rds"} for multiple types.}

\item{other}{Character. Additional case-insensitive substring the filename
must contain (default: \code{""} matches everything).}

\item{dir}{Character. Directory to search. Defaults to the current working
directory if \code{NULL}.}
}
\value{
Character vector of matching filenames, sorted reverse
alphabetically. \code{character(0)} if none match or \code{dir} does not exist.
}
\description{
Returns filenames (not full paths) in \code{dir} that match all of \code{search},
\code{type}, and \code{other} patterns. Results are sorted in reverse alphabetical
order so the most recently dated filename (by convention \verb{_YYYY-MM-DD})
appears first.
}
\examples{
list_dir_files(search = "Formulas_", type = "xlsx|rds", dir = tempdir())
}
