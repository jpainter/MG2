% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outliers.R
\name{unseasonal}
\alias{unseasonal}
\title{Detect Seasonally-Adjusted Outliers}
\usage{
unseasonal(
  x,
  smallThreshold = 100,
  deviation = 3,
  logical = FALSE,
  interpolate = FALSE,
  .lambda = 0.5,
  .progress = FALSE,
  total = NA
)
}
\arguments{
\item{x}{Numeric vector (one time series).}

\item{smallThreshold}{Numeric. Skip series with all values ≤ this
(default: \code{100}).}

\item{deviation}{Numeric. MAD multiplier for outlier threshold (default: \code{3}).}

\item{logical}{Logical. Return logical flag when \code{TRUE}, cleaned series when
\code{FALSE} (default: \code{FALSE}).}

\item{interpolate}{Logical. Interpolate missing values (default: \code{FALSE}).}

\item{.lambda}{Numeric. Box-Cox lambda passed to \code{tsclean} (default: \code{0.5}).}

\item{.progress}{Logical. Update Shiny progress bar (default: \code{FALSE}).}

\item{total}{Integer. Total series count for progress increment calculation.}
}
\value{
Logical or numeric vector the same length as \code{x}.
}
\description{
Uses \code{forecast::tsclean()} to produce a seasonally-adjusted forecast for
each series and flags values that deviate from the forecast by more than
\verb{deviation × MAD}.
}
