% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo.R
\name{mg2_demo_setup}
\alias{mg2_demo_setup}
\title{Set Up Demo Data Directory}
\usage{
mg2_demo_setup(
  dir = file.path(path.expand("~"), "mg2_demo"),
  overwrite = FALSE
)
}
\arguments{
\item{dir}{Path to the directory to create (or reuse). Defaults to
\verb{~/mg2_demo}. The directory is created if it does not exist.}

\item{overwrite}{Logical. If \code{TRUE}, overwrites existing files in \code{dir}
(default \code{FALSE} — skips files that already exist).}
}
\value{
The path to \code{dir}, invisibly.
}
\description{
Writes the MG2 demo datasets (Sierra Leone malaria, 5 elements, 60 months)
to a local directory in the format the Shiny app expects: a formula \code{.xlsx},
a metadata \code{.rds}, and a processed dataset \code{.rds}.
}
\details{
After running this function, open the app with \code{\link[=run_mg2]{run_mg2()}}, set the
directory to the path returned, and skip the Login step — the demo data
is ready to explore from the Data tab onward.
}
\examples{
\dontrun{
mg2_demo_setup()        # writes to ~/mg2_demo
run_mg2()               # open the app, paste ~/mg2_demo as the directory
}
}
