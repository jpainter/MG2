% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/burden_functions.R
\name{run_burden_estimates}
\alias{run_burden_estimates}
\title{Run all requested burden estimation methods}
\usage{
run_burden_estimates(
  data,
  target_elements,
  region_col,
  methods = c("A", "B", "C1", "C2", "E"),
  attendance_elements = NULL,
  tested_elements = NULL,
  population_element = NULL,
  cat_map_attend = NULL,
  cat_map_tested = NULL,
  beta_young = 0.73,
  beta_old = 0.57,
  alpha = 0.48,
  gamma_young = 2,
  gamma_old = 1,
  lambdamin = 0.75,
  run_d = FALSE,
  care_seeking = c(default = 0.8),
  default_p = 0.8,
  n_bootstrap = 1000L,
  verbose = TRUE
)
}
\arguments{
\item{data}{data.table from \code{selectedData()} — must have columns \code{orgUnit},
\code{Month}, \code{data}, \code{value}, \code{Selected}, and a region column.}

\item{target_elements}{character; \code{data} values for the outcome (e.g.
confirmed malaria cases).}

\item{region_col}{character; name of the region column (typically
\code{levelNames[2]}).}

\item{methods}{character vector of methods to run.  Any subset of
\code{c("A","B","C1","C2","E")}.  Default: all five.}

\item{attendance_elements}{character or NULL; required for methods B and E.}

\item{tested_elements}{character or NULL; required for method E.}

\item{population_element}{character or NULL; triggers per-100k rates for all
methods and the incidence output for method E.}

\item{cat_map_attend}{named character; confirmed → attendance category map.}

\item{cat_map_tested}{named character; confirmed → tested category map.}

\item{beta_young, beta_old}{numeric; β for method E (default Thwing 2020).}

\item{alpha}{numeric; α for method E (default 0.48).}

\item{gamma_young, gamma_old}{numeric; γ for method E (default 2.0 / 1.0).}

\item{lambdamin}{numeric; λmin for method E (default 0.75).}

\item{run_d}{logical; whether to apply the care-seeking adjustment (D).}

\item{care_seeking}{named numeric; care-seeking proportions by category for D.}

\item{default_p}{numeric; fallback care-seeking proportion for D.}

\item{n_bootstrap}{integer; bootstrap draws (default 1000).}

\item{verbose}{logical; print progress messages (default TRUE).}
}
\value{
A list with elements:
\describe{
\item{\code{A}, \code{B}, \code{C1}, \code{C2}, \code{E}}{Results from each method (if run),
each a \verb{list($subnational, $national)}.}
\item{\code{D}}{Care-seeking adjusted table (if \code{run_d = TRUE}).}
\item{\code{summary_national}}{Wide data.frame of national totals,
all methods side by side.}
\item{\code{summary_subnational}}{Wide data.frame of sub-national totals.}
\item{\code{log}}{Character vector of timestamped progress messages.}
}
}
\description{
A non-Shiny entry point that calls each requested method in sequence and
returns a named list of results suitable for scripted analysis or testing.
}
